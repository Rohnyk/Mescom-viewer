services:
  mescom-dashboard:
    build: .
    container_name: mescom-dashboard
    ports:
      - "35000:35000"
    volumes:
      - mescom-data:/app/data
    restart: unless-stopped
    env_file:
      - .env

volumes:
  mescom-data:
    driver: local
